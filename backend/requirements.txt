fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
pydantic[email]>=2.6.0
pydantic-settings>=2.2.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
google-auth>=2.28.0
requests>=2.31.0
aiosmtplib>=3.0.0
slowapi>=0.1.9
websockets>=12.0
# Pinned: the crosstabs/MASD pipeline outputs were validated byte-for-byte on
# exactly these versions, and behaviour drifts across releases (e.g. an empty
# to_excel sheet_name defaults to "Sheet1" on this stack but raises on newer
# ones). Bump deliberately, re-running a full pipeline baseline comparison.
openpyxl==3.1.5
# Data-analytics pipelines (admin "Database" section — backend/pipelines/)
pandas==3.0.5
numpy==2.4.6
# Web push notifications (PWA)
pywebpush>=2.0.0
# Cloudflare R2 media storage (S3-compatible API) - app/storage.py
boto3>=1.34.0

# Virtual Environments
venv/
backend/venv/
.venv/

# Dependencies
node_modules/
frontend/node_modules/

# Build outputs
dist/
frontend/dist/

# Secret files
.env
.env.production
backend/.env
frontend/.env

# Python caching
__pycache__/
backend/app/__pycache__/
backend/app/routers/__pycache__/
*.pyc

# System / IDE files
.DS_Store
.gemini/
.system_generated/
browser/
*.log
gh
gh.tar.gz

# Local dev artifacts (integration branch)
backend/venv-win/
*.db
backend/nurturehub.db
.claude/
frontend/.env.local

# pgbouncer auth file holds the DB credential/verifier — never commit it
deploy/pgbouncer/userlist.txt

# Admin form-builder media uploads (user content, served at /uploads)
backend/uploads/

# Form-definition refresh backups (app.refresh_form_definitions)
backend/form_backups/

# Data-analytics pipeline runtime data (admin uploads, run workspaces, outputs)
backend/pipeline_data/

# Audit spool — the on-disk fallback for audit events when the database is
# briefly unavailable. Contains real access records; never commit it.
audit_spool.jsonl
backend/audit_spool.jsonl

# Local interview-prep notes — not part of the platform.
HLD_Interview_Guide.html
LLD_Interview_Guide.html
NurtureHUB_Backend_Interview_Guide.html
NurtureHUB_Diagrams.html
